<a href="/songs">Dashboard</a>

<h2><%= @user.first_name %> <%= @user.last_name %>'s Playlist</h2>

<table>
		<thead>
			<tr>
				<th>Artist</th>
				<th>Title</th>
				<th>Times Added</th>
			</tr>
		</thead>
		<tbody>
			<% @playlist.each do |listener| %>
				<tr>
					<td><%= listener.song.artist %></td>
					<td><a href="/songs/<%= listener.song.id %>"><%= listener.song.title %></a></td>
					<td><%= listener.adds %></td>
				</tr>
			<% end %>
		</tbody>
	</table>